<%= form_with(model: project_module, local: true) do |form| %>
  <% if project_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_module.errors.count, "error") %> prohibited this project_module from being saved:</h2>

      <ul>
      <% project_module.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :associated_project %>
    <%= form.select :project_id, options_for_select(@projects.collect{ | project | [project.name, project.id] }) %>
  </div>

  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :start_date %>
    <%= content_tag :div, :class => "date_select", :data => {:provide => 'datepicker', 'date-format' => 'yyyy-mm-dd', 'date-autoclose' => 'true'} do
      form.text_field :start_date, class: 'form-control', placeholder: 'YYYY-MM-DD', value: Time.now.strftime('%Y-%m-%d')
    end%>
  </div>

  <div>
    <%= form.label :completion_goal_date %>
    <%= content_tag :div, :class => "date_select", :data => {:provide => 'datepicker', 'date-format' => 'yyyy-mm-dd', 'date-autoclose' => 'true'} do
      form.text_field :completion_goal_date, class: 'form-control', placeholder: 'YYYY-MM-DD', value: Time.now.strftime('%Y-%m-%d')
    end%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
